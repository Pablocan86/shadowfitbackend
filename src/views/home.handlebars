<div>
  {{#if usuario}}
    <p>Nombre: {{usuario.nombre}}</p>
    <p>Apellido: {{usuario.apellido}}</p>
    <form action="/api/session/logout" method="POST"><button
        type="submit"
      >Cerrar sesión</button></form>
  {{else}}
    <p>No existe usuario iniciado</p>
  {{/if}}
</div>